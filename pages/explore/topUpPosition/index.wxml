<view class="mode" style="margin-top: {{tabbarRealHeight  + 'px'}}; height: calc(100vh - {{tabbarRealHeight +90 + 'px'}});">
  <view class="tabsList">
    <view 
      class="{{activeIndex == item.id ? 'tabs_active': ''}}" 
      wx:for="{{tabsList}}" wx:key="index" 
      bind:tap="changeTabs" 
      data-id="{{item.id}}"
    >
      {{item.title}}
    </view>
  </view>
	<view wx:if="{{activeIndex == 0}}">
		
	</view>
	<view wx:if="{{activeIndex == 1}}">
		
	</view>
</view>